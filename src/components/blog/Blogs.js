import React from 'react';
import Header from '../shared/Header';

const Blogs = () => {
    return (
        <div>
            <Header>
                
            </Header>
        </div>
    );
};

export default Blogs;